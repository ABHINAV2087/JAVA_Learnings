public class Foreachloop {
    public static void main(String[] args) {
        int[] arr = {10,20,30,40};
        for(int a : arr)
        System.out.println(a);
    }
    
}

// this loop is always used arrays and collections

// it always start from 0th index of the array 

// it always passes through every element of the array 

// we cannot run this in reverse order

//the var declared should either match array type it should be higher type
